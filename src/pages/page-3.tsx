import React from "react"
import Select from "react-select"
import { useDoc } from "../../useDoc"

function TopThree({ location }) {
  console.log(location)
  return <p>h1</p>
}

export default TopThree
